# 🍽️ Βάση Δεδομένων MariaDB – restaurant_booking

Αυτός ο φάκελος περιέχει την εξαγόμενη βάση δεδομένων MariaDB που χρησιμοποιείται στην εφαρμογή **App-Κρατήσεων**.

---

restaurant_booking:



---

##  Περιεχόμενο φακέλου

- `restaurant_booking.sql` → αρχείο εξαγωγής (`dump`) της βάσης.
- `README.md` → το παρόν αρχείο οδηγιών.

---

##  Προϋποθέσεις

- Εγκατεστημένο το **MariaDB 10.6**
- Πρόσβαση σε τερματικό με δικαιώματα διαχειριστή

---

##  Εισαγωγή βάσης (Import)

1. Άνοιξε **Γραμμή Εντολών (Command Prompt)** ως **διαχειριστής**
2. Εκτέλεσε:

```bash
cd "C:\Program Files\MariaDB 10.6\bin"
# Database - MariaDB

Αυτός ο φάκελος περιέχει το αρχείο SQL που χρησιμοποιείται για την εγκατάσταση της βάσης δεδομένων `restaurant_booking`.

## Περιγραφή

Η βάση δεδομένων περιλαμβάνει πίνακες που σχετίζονται με την εφαρμογή κρατήσεων. Οι πίνακες έχουν δημιουργηθεί και εξαχθεί μέσω του `mysqldump` εργαλείου της MariaDB.

- Όνομα Βάσης: `restaurant_booking`
- Περιεχόμενο: π.χ. πίνακες `users`, `reservations`, κ.λπ.
- Αρχείο: `restaurant_booking.sql`

## Οδηγίες Εισαγωγής

1. Βεβαιώσου ότι έχεις εγκατεστημένη τη **MariaDB** (π.χ. MariaDB 10.6).
2. Άνοιξε Command Prompt ως **Διαχειριστής**.
3. Μετέφερε τον εαυτό σου στο φάκελο `bin`:

```bash
cd "C:\Program Files\MariaDB 10.6\bin"

